[project]
name = "timeseries-model-project"
version = "0.1.0"
description = "Small TimesFM forecasting experiments."
requires-python = ">=3.10"
dependencies = [
  "matplotlib>=3.8",
  "numpy>=1.26.4",
  "timesfm[torch]>=2.0.0",
]

[tool.uv]
package = false
